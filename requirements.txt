# This is the install source for Docker/CI/HF - only what app/, scripts/ and
# tests/ actually import, deliberately unpinned. pyproject.toml (uv) carries a
# larger set on top of this for local dev: data-fetch tooling (kaggle,
# pytubefix, yt-dlp) and experiment tracking (wandb, weave) used by
# notebooks/, none of which serving/training-via-script needs. The two are
# meant to differ - don't "reconcile" them into one list.

# --- training / export ---
ultralytics
torch
torchvision

# --- ONNX toolchain ---
onnx
onnxslim
onnxruntime          # swap for onnxruntime-gpu on CUDA hosts

# --- serving ---
fastapi
uvicorn[standard]
pydantic
python-multipart
prometheus-client

# --- data / eval / misc ---
numpy
opencv-python-headless
pillow
pyyaml
matplotlib
tqdm
requests

# --- load testing (bench/locustfile.py) ---
locust

# --- HF Space demo (app/gradio_app.py) ---
gradio
spaces          # ZeroGPU decorator; no-op outside the HF ZeroGPU host

# --- tests (tests/) ---
pytest
pytest-asyncio
httpx           # FastAPI's TestClient needs this installed separately